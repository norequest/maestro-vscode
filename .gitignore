node_modules/
dist/
packages/*/dist/
.hallucinate/.runtime/
.DS_Store
**/.DS_Store

# VS Code integration test harness
out/
packages/*/out/
.vscode-test/
packages/*/.vscode-test/

# Packaged extension build artifacts
*.vsix

# Claude Code local agent worktrees (scratch)
.claude/

# Dogfood scaffold output: running the extension with packages/extension as the
# workspace scaffolds vendored skills/config into these dirs. They are runtime
# byproducts, not source.
packages/extension/.hallucinate/
packages/extension/.github/

# Internal docs (runbooks, design refs) kept local, not published to the repo
/docs/

# Local skill scaffold artifact (canonical source: packages/config/src/vendored-skills.ts)
/.github/skills/
