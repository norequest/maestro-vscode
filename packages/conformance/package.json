{
  "name": "@maestro/conformance",
  "version": "0.0.0",
  "private": true,
  "description": "Test-only, source-resolved package. It is intentionally NOT built: adapter conformance tests import it via source, so main/types deliberately point at src/index.ts (not dist). Do not add a build step or switch to dist; that would break how the adapter tests resolve this harness.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@maestro/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
