{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Hallucinate (dogfood on this repo)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "${workspaceFolder}/../..",
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "build hallucinate"
    }
  ]
}
